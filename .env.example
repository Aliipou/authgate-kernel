# Copy to .env — never commit .env
APP_ENV=development
PORT=8000

# REQUIRED for /readyz and mutating endpoints
AUTHGATE_ADMIN_TOKEN=change-me-in-production

# Audit JSONL (compose mounts /data)
AUTHGATE_AUDIT_PATH=/data/audit.jsonl

AUTHGATE_BACKEND=python
